import Config

# Default configuration for JidoHetzner.
# These values are used as fallbacks when not provided at runtime.
#
# config :jido_hetzner,
#   api_token: {:system, "HETZNER_API_TOKEN"},
#   server_type: "cx22",
#   image: "ubuntu-24.04",
#   location: "fsn1"
